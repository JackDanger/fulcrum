//! STUB — see contract in ratio/mod.rs. Worker fills this file.
use super::TokenStream;
pub fn extract_gz(_gz: &[u8]) -> Result<(TokenStream, Vec<u8>), String> { todo!() }
pub fn inflate_gz(_gz: &[u8]) -> Result<Vec<u8>, String> { todo!() }
